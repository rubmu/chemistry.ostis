lib_component_agent_add_acid
=> nrel_main_idtf:
	[Компонент библиотеки. sc-агент определения того, что будет, если добавить кислоту]
	(* <- lang_ru;; *);
	[Library component. sc-agent determine what will happen if add acid]
	(* <- lang_en;; *);

<- library_of_platform_independent_reusable_components;
<- library_of_atomic_reusable_components;

=> nrel_attendant_component:
	lib_component_ui_menu_add_acid;

-> rrel_key_sc_element:
	.platform_independent_realization_of_sc_agent_add_acid;

<- rrel_key_sc_element:
	...
	(*
	<- explanation;;
	<= nrel_sc_text_translation:
		...
		(*
		-> rrel_example:
			"file://htmls/domains.html"
				(* <- lang_ru;; *);;
		*);;
	*);;

sc_node_not_relation -> action_add_acid;;

action_add_acid => nrel_main_idtf:
	[действие. определение того, что будет, если добавить кислоту]
	(* <- lang_ru;;	*);
	[action. Dtermine what will happen if add acid]
	(* <- lang_en;;	*);;
